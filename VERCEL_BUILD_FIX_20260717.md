# Vercel build düzeltmesi — 17.07.2026

- `CalculationWorkbench.tsx` içindeki tarayıcı zamanlayıcısı `number | null` olarak düzeltildi.
- `window.setTimeout()` dönüş tipi ile Node.js `Timeout` tipi arasındaki çakışma kaldırıldı.
- `package.json` Node.js motoru `24.x` olarak güncellendi.
- `.nvmrc` değeri `24` olarak güncellendi.
- Mevcut URL ve Analytics kimliği değiştirilmedi.
